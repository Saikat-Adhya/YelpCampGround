<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= campgrounds.title %></title>
    <!-- Bootstrap CSS CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container">
            <!-- <a class="navbar-brand" href="/">Campgrounds</a> -->
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/campgrounds">All Campgrounds</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container mt-5">
        <h1 style="color: rgb(20, 193, 135);">Here is Your Campground:</h1>
        <br>
        <h2 class="text-center mb-4"><%= campgrounds.title %></h2>
        <ul class="list-group">
            <li class="list-group-item"><strong>Location:</strong> <%= campgrounds.location %></li>
            <li class="list-group-item"><strong>Price:</strong> <%= campgrounds.price %></li>
        </ul>

        <div class="text-center mt-4">
            <a href="/campgrounds/<%= campgrounds._id %>/edit" class="btn btn-warning">Edit Campground</a>
        </div>

        <form action="/campgrounds/<%= campgrounds._id %>?_method=DELETE" method="POST" class="text-center mt-2">
            <button type="submit" class="btn btn-danger">Delete Campground</button>
        </form>

        <div class="text-center mt-4">
            <a href="/campgrounds" class="btn btn-secondary">View All Campgrounds</a>
        </div>
    </div>

    <!-- Bootstrap JS and Popper.js CDN -->
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.min.js"></script>
</body>
</html>
