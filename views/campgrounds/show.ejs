<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shows By Id</title>
</head>
<body>
    <h1><%= campgrounds.title %></h1>
    <li><%= campgrounds.location %></li>
    <li><%= campgrounds.price %></li>
    <p><a href="/campgrounds/<%= campgrounds._id %>/edit">Edit Campground</a></p>
    <p>
        <form action="/campgrounds/<%= campgrounds._id %>?_method=DELETE" method="POST">
            <button>Delete Campground</button>
        </form>
    </p>
    <a href="/campgrounds">All Campgrounds</a>
</body>
</html>