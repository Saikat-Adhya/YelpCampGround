<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Campgrounds</title>
    <!-- Bootstrap CSS CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* Global Styles */
        body {
            background-color: #121212; /* Dark background for the page */
            color: #e4e4e4; /* Light text color for readability */
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        .navbar {
            background-color: #1f1f1f; /* Dark navbar background */
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3); /* Soft shadow */
        }

        .navbar-brand {
            color: #20c997;
            font-weight: 700;
        }

        .navbar-nav .nav-link {
            color: #e4e4e4 !important;
            font-weight: bold;
        }

        .navbar-nav .nav-link:hover {
            color: #a7d8c2 !important;
        }

        h1 {
            color: #00f1d3; /* Aqua color for the title */
            text-align: center;
            font-size: 3rem;
            font-weight: 700;
            margin-top: 50px;
            text-shadow: 2px 2px 10px rgba(0, 241, 211, 0.7);
        }

        h3 {
            color: #c1c1c1; /* Light gray for subtitle */
            text-align: center;
            font-size: 1.5rem;
            margin-top: 20px;
        }

        .card {
            background-color: #2c2c2c; /* Dark background for cards */
            border: 1px solid #444444; /* Dark border */
            border-radius: 12px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.5);
        }

        .card-body {
            color: #e4e4e4;
        }

        .card-title {
            color: #20c997; /* Light green for card titles */
            font-size: 1.5rem;
            font-weight: 600;
        }

        .card-text {
            color: #b8b8b8; /* Light gray for text */
            font-size: 1.2rem;
        }

        .btn-primary {
            background-color: #4e73df; /* Soft blue for buttons */
            border-color: #4e73df;
        }

        .btn-primary:hover {
            background-color: #3b5bdc;
            border-color: #3b5bdc;
        }

        .btn-success {
            background-color: #28a745; /* Green button for creating new campgrounds */
            border-color: #28a745;
        }

        .btn-success:hover {
            background-color: #218838;
            border-color: #1e7e34;
        }

        .container {
            max-width: 1000px;
            margin-top: 50px;
        }

        .footer {
            background-color: #1f1f1f;
            color: #ffffff;
            padding: 15px;
            text-align: center;
            font-weight: 600;
            position: fixed;
            width: 100%;
            bottom: 0;
        }

        .footer p {
            margin: 0;
        }

        .nav-link.btn-link {
            font-weight: bold;
            color: rgb(239, 24, 24);
            background-color: rgb(113, 201, 113);
        }

        .nav-link.btn-link:hover {
            color: #ff6b6b;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light">
        <div class="container">
            <a class="navbar-brand" href="/dashboard">Campgrounds</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/campgrounds/new">Create New Campground</a>
                    </li>
                    <% if (user) { %> <!-- Check if user is logged in -->
                        <li class="nav-item">
                            <form action="/logout" method="GET" style="display:inline;">
                                <button type="submit" class="nav-link btn btn-link" style="font-weight: bold;">Logout</button>
                            </form>
                        </li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container text-center mt-5">
        <h1 class="text-decoration-underline"><b>All Campgrounds</b></h1>
        <h3 class="mt-4"><i><b>Created Campgrounds:</b></i></h3>

        <div class="row mt-4">
            <% for (let campground of campgrounds) { %>
                <div class="col-md-4 mb-4">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title"><%= campground.title %></h5>
                            <p class="card-text"><strong>Location:</strong> <%= campground.location %></p>
                            <a href="/campgrounds/<%= campground._id %>" class="btn btn-primary">View Details</a>
                        </div>
                    </div>
                </div>
            <% } %> 
        </div>

        <div class="mt-4">
            <a href="/campgrounds/new" class="btn btn-success btn-lg">Create New Campground</a>
        </div>
    </div>

    <div class="footer">
        <p>&copy; 2024 Campgrounds. All rights reserved.</p>
    </div>

    <!-- Bootstrap JS and Popper.js CDN -->
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.min.js"></script>
</body>
</html>
