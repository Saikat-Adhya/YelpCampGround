<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All CampGrounds</title>
</head>
<style>
    .body {
        background-color: rgb(190, 245, 245);
        display: flex;
        flex-direction: column; /* Stack items vertically */
        align-items: center; /* Center items horizontally */
        justify-content: center; /* Center items vertically */
        height: 100vh; /* Make sure the body takes full viewport height */
        margin: 0; /* Remove default margin */
    }
    ul {
        list-style-type: none; /* Remove bullet points */
        padding: 0; /* Remove default padding */
    }
    li {
        margin: 10px 0; /* Add some spacing between list items */
    }
    button{
        padding: 10px 20px;
        border: 3px solid red;
        font-size: 20px;
        border-radius: 100px;
        font-weight: bold;
        cursor: pointer;
    }
    button:hover{
        background-color: rgb(158, 136, 180);
        color: aliceblue;
    }
    li a{
        font-size: 25px;
    }
</style>
<body class="body">
    <h1><u><b>All campgrounds</b></u></h1>
    <ul>
        <h3><i><b>Created Campgrounds:-</b></i></h3>
        <% for(let campground of campgrounds) {%>
            <li><a href="/campgrounds/<%= campground._id %>"><%= campground.title %></a></li>
        <% } %> 
    </ul>

        <button><a href="/campgrounds/new">Create Campground</a></button>
</body>
</html>